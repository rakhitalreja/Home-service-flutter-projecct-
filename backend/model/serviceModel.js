const db = require('../db'); const ServiceModel = { getAll: (callback) => { db.query('SELECT * FROM services', callback); }, getById: (id, callback) => { db.query('SELECT * FROM services WHERE id = ?', [id], callback); }, create: (data, callback) => { const { name, description, price } = data; db.query('INSERT INTO services (name, description, price) VALUES (?, ?, ?)', [name, description, price], callback); }, update: (id, data, callback) => { const { name, description, price } = data; db.query('UPDATE services SET name=?, description=?, price=? WHERE id=?', [name, description, price, id], callback); }, delete: (id, callback) => { db.query('DELETE FROM services WHERE id = ?', [id], callback); }, }; module.exports = ServiceModel;
