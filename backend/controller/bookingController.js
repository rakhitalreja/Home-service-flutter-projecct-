const BookingModel = require('../model/bookingModel'); const BookingController = { getAll: (req, res) => { BookingModel.getAll((err, results) => { if (err) return res.status(500).json({ error: err.message }); res.json(results); }); }, getById: (req, res) => { BookingModel.getById(req.params.id, (err, results) => { if (err) return res.status(500).json({ error: err.message }); res.json(results[0]); }); }, create: (req, res) => { BookingModel.create(req.body, (err, results) => { if (err) return res.status(500).json({ error: err.message }); res.json({ message: 'Booking created!', id: results.insertId }); }); }, update: (req, res) => { BookingModel.update(req.params.id, req.body, (err) => { if (err) return res.status(500).json({ error: err.message }); res.json({ message: 'Booking updated!' }); }); }, delete: (req, res) => { BookingModel.delete(req.params.id, (err) => { if (err) return res.status(500).json({ error: err.message }); res.json({ message: 'Booking deleted!' }); }); }, }; module.exports = BookingController;
