const ServiceModel = require('../model/serviceModel'); const ServiceController = { getAll: (req, res) => { ServiceModel.getAll((err, results) => { if (err) return res.status(500).json({ error: err.message }); res.json(results); }); }, getById: (req, res) => { ServiceModel.getById(req.params.id, (err, results) => { if (err) return res.status(500).json({ error: err.message }); res.json(results[0]); }); }, create: (req, res) => { ServiceModel.create(req.body, (err, results) => { if (err) return res.status(500).json({ error: err.message }); res.json({ message: 'Service created!', id: results.insertId }); }); }, update: (req, res) => { ServiceModel.update(req.params.id, req.body, (err) => { if (err) return res.status(500).json({ error: err.message }); res.json({ message: 'Service updated!' }); }); }, delete: (req, res) => { ServiceModel.delete(req.params.id, (err) => { if (err) return res.status(500).json({ error: err.message }); res.json({ message: 'Service deleted!' }); }); }, }; module.exports = ServiceController;
